$('.good-list').empty().append("<%= j render partial: 'goods' %>");
$('.good-edit').empty().append("<%= j render partial: 'index' %>");

var $dialog = $('#good-success');
$dialog.show();
$dialog.find('.weui_btn_dialog').on('click', function () {
   $dialog.hide();
});
